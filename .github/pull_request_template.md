## Summary

<!-- What changed and why -->

## Checklist

- [ ] `npm run test` / `bun test` passes (QR round-trip decode)
- [ ] `bun run firefox:build` passes `web-ext lint` locally
- [ ] No new permissions requested in `extension/manifest.json`
- [ ] `version` in `extension/manifest.json` not bumped (release-only change)
